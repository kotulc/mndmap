version: 2

source:
  root: docs
  include: ["**/*.{md,mdx}"]

destination: site

map:
  section: { depth: 2, beyond: body }
  table: { as: body, header: row }

overrides:
  - under: ["Payload handling", "Requirements"]
    map:
      table: { as: rows, type: req.requirement, key: id }
